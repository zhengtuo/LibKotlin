<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.drelovey.realize">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".app.DreloveyApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.Light.NoActionBar"
        tools:replace="android:allowBackup,android:theme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name="com.drelovey.realize.ui.main.view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.lib.view.ConstraintLayoutActivity" />

        <activity android:name=".ui.lib.view.WhatIfActivity" />

        <activity android:name=".ui.lib.view.CoroutinesActivity" />

        <activity android:name=".ui.lib.view.DrawableDslActivity" />

        <activity android:name=".ui.learn.view.MeasureLayoutActivity" />

        <activity android:name=".ui.learn.view.ShaderActivity" />

    </application>

</manifest>